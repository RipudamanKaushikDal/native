<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Reardo.Pages.ComicPages"
             BackgroundColor="Black">

    
    
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <Button Text="Load Images" HorizontalOptions="Center" HeightRequest="50" WidthRequest="100"  BackgroundColor="HotPink" Command="{Binding DownloadPages}"/>
            <CollectionView x:Name="PagesDisplay"  EmptyView="No data" ItemsSource="{Binding ChapterImages}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Padding="5">
                            <Image Source="{Binding PageImage}" Aspect="AspectFill" HeightRequest="300"   />
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>