<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Reardo.Pages.SeriesDetail">
    <ContentPage.Content>
        <StackLayout Padding="10" Orientation="Vertical">
            <Button Text="Show Chapters" x:Name="DisplayContent" WidthRequest="60" BackgroundColor="AliceBlue" Clicked="DisplayContent_Clicked"/>
            <CollectionView x:Name="ChapterDisplay" SelectionMode="Single">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="80" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            
                            <Label Text="{Binding ChapterName}" FontSize="Title" Grid.Row="0"/>
                            <Label Text="{Binding UpdatedDate}" FontSize="Subtitle" TextColor="Gray" Grid.Row="0"/>

                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>